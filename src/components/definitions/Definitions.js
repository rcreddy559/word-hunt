export default function Definitions(meanings, language, word) {
  console.log(`word: ${meanings[0]}`);

  return (
    <div>
      <div className="subTitle">Start by typing a word in Search</div>
    </div>
  );
}
